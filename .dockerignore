# Dependencies (installed fresh inside the build stage via npm ci)
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output (built fresh inside the build stage)
dist
build

# Version control
.git
.gitignore

# IDE and editor files
.vscode
.idea
*.swp
*.swo
*~
.DS_Store

# Documentation (not needed inside the image)
*.md
docs
LICENSE

# Testing and CI
coverage
.nyc_output
.github
.gitlab-ci.yml
.travis.yml

# Docker files (not needed inside the image)
Dockerfile
docker-compose.yml
.dockerignore

# Environment files
.env
.env.local
.env.*.local

# Playwright
.playwright-cli
playwright-report
test-results

# Miscellaneous
*.log
*.tmp
*.temp
.thumbs.db
